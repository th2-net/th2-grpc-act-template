name: Scan licenses and vulnerabilities in project

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 1'

jobs:
  java-scan:
    uses: th2-net/.github/.github/workflows/compound-java-scan.yml@main
    secrets:
      nvd-api-key: ${{ secrets.NVD_APIKEY }}
      cisa-domain: ${{ secrets.CISA_DOMAIN }}
      cisa-user: ${{ secrets.CISA_USER }}
      cisa-password: ${{ secrets.CISA_PWD }}
  python-scan:
    name: Vulnerabilities scan
    uses: th2-net/.github/.github/workflows/python-scan.yml@main